---
import {Image} from 'astro:assets'
const {source, caption, index} = Astro.props
---
<figure>
    <Image src={source} alt={caption} width={950}/>
    <figcaption><strong>Figure {index + caption && caption.length > 0 ? ': ' : ''}</strong>{caption ? caption : ''}</figcaption>
</figure>
